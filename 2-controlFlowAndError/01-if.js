// * Conditional
// If this, then this

let isOn = true;

if (isOn = true);
console.log("The light is on.");

// the condition default is always set to true

if (isOn) {
    console.log("The light is on.");
}
// same statement as above because JS assumes things are true out of the gate

let isOff = false;
 if (isOff == false) {
    console.log("The light is off!");
 }

 let weather = 65;

 if (weather < 70) {
     console.log("Wear a jacket today!");
 }
// if the statement isn't true, it just doesn't run the code. 

